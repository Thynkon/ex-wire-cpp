#ifndef ICMP_H

#define ICMP_H

/* class IcmpPacket : public Packet { */
/*
friend ostream &operator<<(ostream &os, const IcmpPacket &packet) {
  os << "ICMP " << srcip << " -> " << dstip << endl;
  os << iphdrInfo << endl;
  os << "Type:" << icpm_header->icmp_type
     << " Code:" << icpm_header->icmp_code
     << " ID:" << ntohs(icpm_header->icmp_hun.ih_idseq.icd_id)
     << " Seq:" << ntohs(icpm_header->icmp_hun.ih_idseq.icd_seq) << endl;
  os << "+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+-+"
     << endl;
  return os;
}
*/

#endif